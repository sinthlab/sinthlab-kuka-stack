__pycache__/
# Recorded trial data -- never version-controlled. One folder per launch (analysis/expt_<run>_<timestamp>/),
# plus any loose CSV / sidecar in analysis/ (the restricted-plane fixture writes its CSV there).
analysis/*.csv
analysis/*.meta.json
analysis/expt_*/
docs/
# Dashboard (experiment_ctrl_gui): per-run parameter copies and launch logs, one per Start.
experiment_ctrl_gui/runs/
experiment_ctrl_gui/logs/
