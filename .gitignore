__pycache__/
# Recorded trial data -- never version-controlled. One folder per launch since the recorder
# moved to analysis/expt_<run>_<timestamp>/; the flat patterns cover recordings made before that.
analysis/*.csv
analysis/*.meta.json
analysis/expt_*/
docs/
